#include "mbedtls/include/mbedtls/ecjpake.h"
// #include "mbedtls/entropy.h"
// #include "mbedtls/ctr_drbg.h"
#include <string>
#include <napi.h>
using namespace std;
// Struct I need
// mbedtls_ecjpake_context

// Functions I need
//  mbedtls_ecjpake_init(mbedtls_ecjpake_context *ctx)
//  mbedtls_ecjpake_write_round_one(mbedtls_ecjpake_context *ctx,
//                                  unsigned char *buf, size_t len, size_t *olen,
//                                  int (*f_rng)(void *, unsigned char *, size_t),
//                                  void *p_rng)

// class JPakeAddon
// {

// }